# Security

Do not commit webhook URLs, private API keys, passwords, or internal feed links.

If you use webhook posting, pass the URL through `DIGEST_WEBHOOK_URL` or your deployment platform's secret store.

To report a security issue, open a private advisory or contact the repository owner directly.
